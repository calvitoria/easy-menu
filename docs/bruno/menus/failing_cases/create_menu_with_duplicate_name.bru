meta {
  name: create_menu_with_duplicate_name
  type: http
  seq: 2
}

post {
  url: {{host}}/restaurants/{{restaurant_id}}/menus
  body: json
  auth: none
}

body:json {
  {
    "name": "My super menu"
  }
}

docs {
  This request is expected to fail if a menu with the name "My super menu" already exists.
  Run the `create_menu` request first.
}
