meta {
  name: Get Single Menu Item
  type: http
  seq: 3
}

get {
  url: {{host}}/menu_items/{{menu_item_id}}
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Menu item has id", function() {
    expect(res.body).to.have.property("id");
  });
}
