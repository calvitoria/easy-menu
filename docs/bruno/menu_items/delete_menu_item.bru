meta {
  name: Delete Menu Item
  type: http
  seq: 5
}

delete {
  url: {{host}}/menu_items/{{menu_item_id}}
  body: none
  auth: none
}

tests {
  test("Status code is 204", function() {
    expect(res.status).to.equal(204);
  });
}
