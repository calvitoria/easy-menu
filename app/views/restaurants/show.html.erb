<div class="px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <div class="flex items-center">
        <div class="h-16 w-16 rounded-full bg-indigo-100 flex items-center justify-center mr-4">
          <span class="text-indigo-600 font-bold text-2xl"><%= @restaurant.name[0].upcase %></span>
        </div>
        <div>
          <h1 class="text-3xl font-bold text-gray-900"><%= @restaurant.name %></h1>
          <div class="mt-1 flex items-center space-x-4">
            <span class="text-gray-500">•</span>
            <span class="text-sm text-gray-500"><%= @restaurant.menus.count %> menus</span>
          </div>
        </div>
      </div>
    </div>
    <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none space-x-3">
      <%= link_to 'Edit', edit_restaurant_path(@restaurant), class: "inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
      <%= link_to 'Add Menu', new_restaurant_menu_path(@restaurant), class: "inline-flex items-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
    </div>
  </div>

  <div class="mt-8 grid grid-cols-1 gap-6 lg:grid-cols-2">
    <div class="bg-white shadow sm:rounded-lg">
      <div class="px-4 py-5 sm:px-6">
        <h3 class="text-lg font-medium leading-6 text-gray-900">Restaurant Information</h3>
        <p class="mt-1 max-w-2xl text-sm text-gray-500">Details about the restaurant.</p>
      </div>
      <div class="border-t border-gray-200">
        <dl>
          <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Email</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @restaurant.email %></dd>
          </div>
          <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Address</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @restaurant.address %></dd>
          </div>
          <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Description</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @restaurant.description %></dd>
          </div>
          <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Created</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @restaurant.created_at.strftime('%B %d, %Y') %></dd>
          </div>
        </dl>
      </div>
    </div>

    <div class="bg-white shadow sm:rounded-lg">
      <div class="px-4 py-5 sm:px-6">
        <div class="flex justify-between items-center">
          <div>
            <h3 class="text-lg font-medium leading-6 text-gray-900">Menus</h3>
            <p class="mt-1 max-w-2xl text-sm text-gray-500">Available menus for this restaurant.</p>
          </div>
          <%= link_to 'Add Menu', new_restaurant_menu_path(@restaurant), class: "inline-flex items-center rounded-md border border-transparent bg-indigo-600 px-3 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
        </div>
      </div>
      <div class="border-t border-gray-200">
        <% if @restaurant.menus.any? %>
  <ul role="list" class="divide-y divide-gray-200">
    <% @restaurant.menus.each do |menu| %>
      <li class="px-4 py-4 sm:px-6 hover:bg-gray-50">
        <div class="grid grid-cols-[1fr_auto_auto_auto] items-center gap-4">
          <div class="min-w-0">
            <p class="truncate text-sm font-medium text-indigo-600">
              <%= menu.name %>
            </p>
            <p class="mt-1 text-sm text-gray-500 truncate">
              <%= truncate(menu.description.to_s, length: 90) %>
            </p>
          </div>

          <%= link_to 'View',
            menu_path(menu),
            class: "text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap" %>

          <%= link_to 'Items',
            menu_menu_items_path(menu),
            class: "text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap" %>

          <span class="text-sm text-gray-400 whitespace-nowrap">
            (<%= menu.menu_items.count %>)
          </span>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>

          <div class="px-4 py-12 text-center">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
            </svg>
            <h3 class="mt-2 text-sm font-medium text-gray-900">No menus</h3>
            <p class="mt-1 text-sm text-gray-500">Get started by creating a new menu.</p>
            <div class="mt-6">
              <%= link_to new_restaurant_menu_path(@restaurant), class: "inline-flex items-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" do %>
                <svg class="-ml-1 mr-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 3a1 1 0 00-1 1v5H4a1 1 0 100 2h5v5a1 1 0 102 0v-5h5a1 1 0 100-2h-5V4a1 1 0 00-1-1z" clip-rule="evenodd" />
                </svg>
                New Menu
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="mt-8 flex justify-end space-x-3">
    <%= link_to 'Back to Restaurants', restaurants_path, class: "inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
  </div>
</div>